# Production dependencies only (no dev/test tools)
# Optimized for container deployment

# Core framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# KyroDB gRPC client
grpcio==1.59.0
protobuf==4.25.0

# Embedding models
sentence-transformers==2.2.2
torch==2.1.0
transformers==4.35.0
Pillow==10.1.0

# LLM client (for reflection generation)
openai==1.3.0

# Billing
stripe==7.7.0

# Customer database
# Note: SQLite is built into Python, no package needed

# Authentication and security
bcrypt==4.1.1
python-jose[cryptography]==3.3.0

# Rate limiting
slowapi==0.1.9

# Observability (Phase 2)
prometheus-client==0.23.1
structlog==25.5.0

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1
cachetools==5.3.2

# HTTP client (for webhooks)
httpx==0.25.1
